<!DOCTYPE html>
<html lang="fr" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Reset password</title>
  </head>
  <body>
    <% include ./partials/messages.ejs %>
    <form class="" action="/user/reset<% if (typeof token != 'undefined') {%>/<%= token %><% } %>" method="post">
      <% if (typeof token == 'undefined') {%> <input type="password" name="oldPassword" placeholder="mot de passe ancien"> <br> <% } %>
      <input type="password" name="password" placeholder="mot de passe"> <br>
      <input type="password" name="confirm" placeholder="confirmer le mot de passe">
      <input type="submit" name="submit" value="Changer">
    </form>
  </body>
</html>
